import {FormUtil} from './FormUtil';


describe('FormUtil', () => { // eslint-disable-line max-lines-per-function
    it('should export', () => {
        expect(typeof FormUtil).toBe('function');
    });
});
